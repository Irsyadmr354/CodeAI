Audit CodeAI 2026-09-04 — vonis + redesign UI

Q: Apakah 5 fitur sudah benar? A: TIDAK. Semua FR gagal penuh/sebagian, terverifikasi independen File:Line.

FR-1 AGENTS 100%: GAGAL. orchestrator.py:57-79 cek substring terbalik — mention benign diblokir, traversal lolos; verify_tool_call tak dipanggil run_pipeline; tanpa precedence Base<System<Global<AGENTS<HOOKS<User. allowance.py:32-35 whitelist prefix tanpa shell-parse — `echo hi && rm -rf ~` APPROVED; blocked 6 literal — `rm -Rf /`, `curl|sh`, `chmod 777` lolos; mode typo fail-open.
FR-2 HOOKS: GAGAL SEBAGIAN. hooks.py:40 fence miss YAML besar; :17 split tanpa list/multiline; :60 exact case-sensitive tanpa deny; :69 filter=='true' miss bool + int(limit) crash + dict bypass; :80 inject tanpa allowlist.
FR-3 Kompresi: GAGAL SEBAGIAN. compactor.py:6 OSC sisa; :22 miss FATAL/Traceback, marker tak dihitung jadi 11>10; :94 placeholder `Summarized N messages` zero-semantik; ast_indexer miss nested + JS indent/default/typed.
FR-4/5 Provider: GAGAL SEBAGIAN. Schema tak standar + tool drop agy/combo (combo.py:178 dict+ChatMessage crash); timeout mislabel 5 provider; model pollution failover gateway.py:169; registry bare->openai vs gateway bare->anthropic; opencode hilang fast-path; hardcoded GOOGLE_CLIENT_SECRET; auth.json 644; codeai.json tanpa compaction + antigravity unreachable.
FR-6/7 Subagent: GAGAL TOTAL. orchestrator.py:116 run_pipeline langsung gateway.chat tanpa spawn; cli.py:243 dispatch langsung; cli.py:757 steer hanya _print; active_subagent_id never set; subagents.py skeleton tanpa worker; diff non-atomic; shell masking bocor + rg injection; loop retry tanpa healing + off-by-one.

UI 12 pain: prompt ganda dead :89, banner 5-baris berisik :121, /model 5 langkah + gate >12 :500, effort nested :559, /login numbering rapuh :277, /combo 4 prompt :627, help flat vs 14 alias, spinner tanpa elapsed :243, steer no-op :757, rich drift 8x, error generik, split+logging konflik.
Redesign lightweight +45/-70 net negatif, zero dep: banner 3-baris `CodeAI v0.1.0 · prov/model · ●n/○m · AGENTS ✓ HOOKS ○`, prompt `prov/model ❯` potong 24char, /model fuzzy satu langkah, effort inline, /login 1-langkah, alias /m /l /p /c /h /s /st /q backward-compat, help 4-grup + did-you-mean, spinner elapsed + steer ◉/✓. Detail mockup+diff: /tmp/opencode/cli-ux-redesign.md, multiprovider: /tmp/opencode/audit-multiprovider-gateway.md, FR6: /tmp/opencode/audit-fr6-fr7.txt.
P0 dulu: allowance shell-parse + steer forward + secret chmod 600. Konfirmasi `yes` untuk implementasi.
