# Debug 3-Bug Ledger — 2026-09-05 (branch loop-eng/debug-3bug-20260905)

Laporan: reasoning tak live; switch antigravity → error Ollama; histori hilang tiap /model. Klarifikasi: via /model picker, sudah /login, harap streaming+spinner, izin debug.
Baseline: 31 passed. Lint/build NOT DETECTED (gap). Rollback OK.

Fix (surgical, stdlib, API preserved):
- gateway.py: trim spasi target/model ("antigravity / x" → normal), parse effort tahan spasi, explicit-target bypass cooldown + fail-fast auth (/login) + error sebut target+sebab+/model, effort low diteruskan. Root cause routing: spasi → lookup miss → jatuh ke Ollama.
- cli.py reasoning: 1 instance Spinner (elapsed hidup 10Hz, dulu recreate→beku); pipeline _on_token + flush per chunk; Live Group(spinner, partial); gateway non-stream → wrapper on_token best-effort + fallback progressive-reveal ≤0.6s; Ctrl-C = steer (worker hidup); non-rich ticker anti-overwrite.
- compactor.py: history/fact_cards session-owned; set_active_model/switch_model label-only; clear() hanya /clear; get/add return copies.
- cli.py wiring delta: _set_active_provider + _do_switch panggil compactor.set_active_model + print "Sesi dipertahankan: N pesan + M ringkasan" + reprint 5 terakhir (≤300 char). Tanpa ini label tak sync + layar kosong dikira clear.

Verifikasi independen: COMPILE_OK; pytest 31 passed (baseline→final, no regresi); secret sweep bersih.
DoD: Correctness/Completeness/Consistency/Guard/RootCause/Env PASS; CONVERGED.
Next: uji manual /model antigravity/low + /model lain cek streaming + histori; lalu merge ke main.

## Wave-2 — Ollama tak terinstall, error menutupi sebab asli (branch sama)

Laporan susulan: pesan akhir tetap "Ollama Connection refused" walau target antigravity. Klarifikasi: Ollama tidak install; visi multi-provider ala OpenCode/Oh-my-pi; user re-login; izin perbaiki.
Root cause ganda: (1) failover mencoba Ollama yang tak ada + pesan akhir hanya tampilkan error terakhir sehingga error asli Antigravity hilang; (2) parse effort tak tahan spasi; (3) label prompt memotong "antigravity" jadi "AI".
Fix:
- gateway.py: skip provider tak tersedia (Errno 111/not installed/no server) sebagai skipped, agregat Attempts chain penuh "antigravity: <sebab> | ollama: skipped", failover lintas CONNECTED saja.
- antigravity.py: parse_model_effort regex tahan spasi (` - low`/` : HIGH`), gemini-3.8-flash-low=(gemini-3.8-flash,low); auth gagal → ProviderError "run /login antigravity" fail-fast; timeout 120 eksplisit. Handoff: gemini.py:200 agy path tanpa --effort + fallback --model kotor.
- cli.py: render chain per baris + saran hanya CONNECTED/running (Ollama disaring bila tak install); label penuh "{provider} ◆", _short hanya untuk model.
Verifikasi independen Wave-2: COMPILE_OK; pytest 31 passed; sweep bersih.
Next: /login antigravity ulang → /model antigravity/gemini-3.8-flash-low → error kini sebut sebab asli bila masih gagal.

## Wave-3 — /login Antigravity via Intercept OAuth (local callback + auto-refresh)

Konfirmasi maksud: Antigravity tanpa API key, auth via OAuth Google + intercept bearer. Pilihan: local callback, auto-refresh, implementasi branch sama.
Riset: backend = Cloud Code Assist (cloudcode-pa.googleapis.com/v1internal:generateContent, envelope {project,model,request}, header Bearer + antigravity UA + Client-Metadata); scope wajib +cclog +experimentsandconfigs; pola Hermes/PR publik (PKCE sendiri + token store sendiri, tanpa secret di repo).
Fix (4 file eksklusif, stdlib):
- google_oauth.py: scope +2; intercept() return dict access+refresh+expires_in (+email); wrapper string compat; state anti-CSRF; bind 127.0.0.1; timeout 15.
- auth_vault.py: store_antigravity_oauth() (epoch-heuristik, default 3600); get_antigravity_token() string/None + auto-refresh preserve email/project; discover_antigravity_project() via loadCodeAssist, parse rekursif, simpan project_id.
- antigravity.py: direct HTTP bearer dulu (model gemini→{base}-{effort}, thinkingConfig hanya thinking, role system→systemInstruction), 401→refresh→retry 1x→fail-fast /login; tanpa token → fallback agy CLI lama.
- cli.py: _login_antigravity via interceptor → store → project → switch; batal/timeout dan client-creds kosong ditangani tanpa crash; fallback sesi agy; tanpa print token.
- tests/test_subscription_providers.py: ekspektasi SCOPES usang (exact 4) → assertIn 6 scope (requirement change, bukan weakening).
Verifikasi independen Wave-3: COMPILE_OK; pytest 31 passed full; sweep bersih.
Catatan jujur: butuh GOOGLE_CLIENT_ID/SECRET (env/vault) untuk exchange; tanpa itu login arahkan set env atau sekali via agy. Direct endpoint mengikuti spec publik yang bisa berubah — bila 404/400, fallback agy tetap jalan.
Next: set kredensial OAuth → /login antigravity → /model GEMINI-3.8 → uji chat.

## Wave-4 — Custom provider in-app + status jujur (tanpa sentuh file eksternal)

Minta: user awam kelola provider dari dalam aplikasi. Pilihan: add+list+remove, validasi penuh, implementasi branch sama.
Fix:
- registry: unregister_custom_provider() (tolak builtin/unknown, False aman) + _local_reachable() single source (TCP 0.5s + /api/tags 1.0s); ollama/lokal ✅ hanya bila reachable — ollama tak terinstall kini ○.
- cli /provider: add (prompt id/baseURL/key tersembunyi, auto GET /v1/models timeout 10, gagal→batal tanpa simpan buta, sukses→config+vault+register+tawar switch), list (tabel + status), remove (khusus custom + konfirmasi + hapus vault), baris /help. Key tak pernah dicetak.
- Penjelasan misteri ✅: gemini ✅ by-design (fallback sesi antigravity); ollama ✅ adalah bug di atas.
Verifikasi: COMPILE_OK; 31 passed; sweep bersih.
Next: /provider add → /model <id>/<model> untuk gateway custom OpenAI-compatible.

## Wave-5 — Revert mesin fallback, combo jadi routing eksplisit + fix picker

Keputusan user: combo = auto-fallback beda nama; semua mesin fallback-ku di-revert, fokus combo. Commit df768a0, push main.
Revert: config allowed_models/free_only (checkout HEAD); registry tier tags (0 diff vs HEAD); cli hapus total helpers tier/allowlist/footer/auto-add + dispatch allow|deny/free-only + kolom/list/help terkait (grep nihil); tests buang 4 safety specs, migrasi _auth_provider dipertahankan. Gateway tak perlu revert (hanya string unify). Dipertahankan: OAuth intercept, /provider in-app+auto-activate, unify /login→/provider, streaming, history, reachable-check, combo hardening.
Fix: picker konsumsi sekuens CSI penuh (panah atas/bawah/home/end + jk + Spasi + Esc murni + live search) + test parser; /combo list (tabel nama+strategi+models) + /combo remove (konfirmasi, tolak unknown).
Verifikasi: 31 passed; grep sisa allowlist/tier nihil; push main OK.

## Wave-6 — Combo FreeAI bocor ke Gemini (trace + fix)

Kecurigaan benar: combo-mu pure free tapi jawaban Gemini asli. Root cause: combo tersimpan bare (FreeAI tanpa slash) sedangkan gateway hanya kenal format combo/FreeAI → deteksi gagal → request jatuh ke failover umum (copilot/gemini/...) → member-level pun tiap member gagal auto-coba non-member → gemini fallback diam-diam ke 3.7-flash via agy. Jadi yang menjawab memang Gemini, bukan member freemu.
Fix commit 8087824: gateway normalisasi combo bare + isolasi member (tanpa failover ke non-member, gagal ya error eksplisit); 12 strategi expose serving_provider/serving_model; gemini unknown fail-closed; /combo use <nama> + /combo edit + footer "dilayani oleh combo X → member aktual". Cara pakai: /combo use FreeAI.
Verifikasi: 31 passed; push main OK. Uji ulang: footer kini buktikan member free yang melayani.

## Wave-7 — Hardening production-ready all-in-one (branch loop-eng/hardening-prod-20260905)

Scope: dead code, logic errors, race condition, UI Indonesia, commit 239e5c1 (belum merge).
Audit temuan: 1 CRITICAL (shell guard None→APPROVED) + HIGH (allowlist bypass, chaining, hooks override, wait tanpa timeout, diff luar workspace, lint chaining palsu) + 16 race (bus/orchestrator/gateway/combo/cli-cache/Live/vault) + 30+ temuan UX bahasa.
Fix Wave-1: allowance fail-closed + path guard + chaining quote-aware; hooks allowlist override + akumulasi; subagents locks + terminate grace; orchestrator timeout+lock+steer bool+spawn pasca-check; tools workspace-bound + mask 10 char + hapus/sisip murni; loop which-based + quote + clamp.
Fix Wave-2: combo validasi+deepcopy+delete bool+load guard+round-robin shared; gateway per-provider RLock (I/O luar lock); vault atomic+flock+600; config konsisten+warning; 15 regression tests baru; cli race (cache lock, patch guard, Live lock, join+steer timeout).
Fix Wave-3: UI full Indonesia + alias (/bantuan /keluar /daftar /riwayat /keadaan /pengaturan /alih), urutan status→label→posisi, error 3 langkah, help kelompok.
Verifikasi: 46 passed (31+15); sweep bersih (hanya definisi regex); konflik overwrite diselaraskan (test+edit).
Next: merge ke main + push (minta izin).
